일반적인 simpleFOC 보드는 2축을 동시에 제어할 수 있지만
3축을 동일한 방식으로 동시에 제어하는 목적이라면 무게중심의 균형과 설계 편의성, 
코드의 동일성 유지를 위해서 1축 제어보드를 사용하는게 편리할 수 있다. 

simpleFOC mega 라는 이름으로 판매되고 있는 보드들은 기존 esp32 simpleFOC 보드와 거의 유사하지만
1축 제어용으로 만들어진 보다 소형화된 보드이다

하지만 하드웨어 구성에 대한 정보는 공개되어 있지 않아, 기존 simplefoc 라이브러리 예제코드사숑시
작동하지 않음

최근 mks simplefoc mega 보드의 회로도가 github에 공개되어 이를 참조로 simplefoc 라이브러리를 사용할 수 있도록 점검하는것을 목표ㅕ로 한다


fdc6287t fet 드라이버는 

pa0 - 32
pb0 - 33
pc0 - 25

32
33
25

ws2812 - io2

vin measure - io13

MKS ESP32 FOC Mega 보드 회로도 분석
7블록(DC INPUT / DC-DC 3.3V / DC-DC 10V / ESP32 / USB to UART / Terminal / Inverter & Current Sense)으로 구성된 3상 FOC 모터 드라이버 보드입니다.

1. DC INPUT (전원 입력)
U1 (DB301V-3.5-2P 터미널) 로 DC 전원 입력
Q1(AP30H80Q, NMOS) + D3(BZT52C12 제너) — NMOS를 이용한 역전압 보호 회로(ideal diode 방식). 게이트가 제너로 클램프되어 역극성 입력 시 MOSFET이 꺼지도록 설계
C7(470uF) + C8~C14 — 대용량 벌크 커패시터 + 다수의 디커플링 커패시터로 입력단 리플 제거
R2/LED1: VIN 인디케이터 LED
2. DC-DC 3.3V / DC-DC 10V (전원 변환)
U3 (XL1509-3.3): VIN → 3.3V 벅 컨버터 (ESP32/로직 전원)
U2 (BL9342): VIN → 10V 컨버터 (R3=10kΩ, R4=120kΩ 피드백 분압으로 출력전압 세팅) — 이 10V가 게이트 드라이버(FD6287T) 전원으로 쓰임
D2(SS24): VBUS(USB) → VIN 방향 OR-ing 다이오드로, USB 전원과 DC 입력 전원을 겸용 가능하게 함
3. ESP32 (메인 컨트롤러)
U5: ESP32-WROOM-32-N8 (플래시 8MB)
R10(7.5kΩ)/R11(1kΩ) 분압 → VIN_MEA: 입력전압을 ADC로 모니터링
LED3 (WS2812B): IO2로 제어되는 RGB 상태 LED
SW1: BOOT/EN 관련 택트 스위치, R9(10kΩ)+C20으로 EN 파워온리셋 지연
IO34/IO35 → CS0_A/CS0_B (전류센서 아날로그 입력, ADC 전용 핀)
PA0/PB0/PC0 → 게이트드라이버 HIN1/2/3 신호
MOSI/MISO/SCLK/CS, SDA0/SCL0 → 외부 확장(SPI/I2C, 예: 각도센서·인코더 연결용으로 추정)
4. USB to UART
U4: CH340C USB-시리얼 브릿지
Q2 (UMH3N 듀얼 트랜지스터) + RTS/DTR → ESP32 표준 오토리셋/부트모드 회로 (일반적인 ESP32 개발보드와 동일한 자동 다운로드 회로)
USBC1 (Type-C), F1(0.5A 퓨즈), D7(SP0503, USB ESD 보호)
5. Terminal (확장 커넥터)
H1(10핀), J2(3핀, A0/B0/C0 — 상전류 출력 신호), J3(4핀), U6(6핀 SPI) 등 외부 센서/모듈 연결용 확장 커넥터 다수
6. Inverter & Current Sense (파워 스테이지, 핵심부)
U9: FD6287T — 3상 하이/로우사이드 게이트 드라이버 IC (부트스트랩 커패시터 C28~C30 포함)
Q3~Q8 (AP30H80Q NMOS ×6) — 3개의 하프브릿지로 구성된 3상 인버터, 각 상마다 10Ω 게이트저항 + 10kΩ 풀다운
R17, R19 (10mΩ 션트저항) — A상, B상 전류 감지 (C상은 키르히호프 법칙으로 계산하는 일반적인 FOC 2-shunt 방식)
U7, U8: INA240A2PWR — 고정밀 전류감지 증폭기, 션트 전압을 증폭해 ESP32 ADC(CS0_A/B)로 전달
C33~C38 (10uF ×6) — 각 하프브릿지 VIN 디커플링

체크할 부분

1순위: M_EN(파워 인에이블) 핀을 코드에서 안 켰을 가능성 — 가장 유력
DC INPUT 블록을 보면 **Q1(AP30H80Q, NMOS)**이 VIN과 PGND 사이에 들어가 있고, 게이트는 R6(1kΩ)/R7(10kΩ) + D3(제너)로 바이어스되며, 이 게이트 신호가 ESP32 쪽 net label 목록에 있는 **M_EN**과 연결되는 구조로 보입니다.

즉 이 보드는 단순 역전압 보호용 MOSFET이 아니라, ESP32 GPIO로 인버터 전원 레일(PGND) 자체를 켜고 끄는 로드스위치를 갖고 있을 가능성이 높습니다. SimpleFOC 오픈루프 예제는 driver.pwm_pins만 설정하고 초기화하지, 이 보드 전용 M_EN 핀은 알지 못하므로 코드에서 별도로 pinMode(M_EN_PIN, OUTPUT); digitalWrite(M_EN_PIN, HIGH);를 안 해주면 PWM은 정상적으로 나가더라도 인버터단 자체에 전원이 아예 안 들어가는 증상(=말씀하신 "전혀 전원이 안 들어옴")과 정확히 일치합니다.

→ 확인 방법: U9(FD6287T)의 VCC(7번)/COM(8번) 핀, 그리고 6개 NMOS의 PGND 쪽에 실제로 전압이 걸리는지 멀티미터로 먼저 확인해보세요. 여기가 0V면 PWM 핀 번호는 상관없이 전원 자체가 안 들어간 겁니다.

2순위: FD6287T는 6개 독립 입력이 필요 — 3PWM 드라이버로는 부족할 수 있음
U9 FD6287T 핀맵을 보면 HIN1/HIN2/HIN3(상측) 뿐 아니라 LIN1#/LIN2#/LIN3#(하측) 입력이 별도로 존재합니다. 이건 IR2101류처럼 1입력으로 상/하보완 자동생성하는 드라이버가 아니라, 상측 3개+하측 3개, 총 6개의 독립 로직 입력이 필요한 타입입니다.

만약 SimpleFOC에서 BLDCDriver3PWM으로 3핀(IO32/33/25)만 지정하고 LIN1-3# 쪽이 아예 제어가 안 되고 있다면, 로우사이드 FET이 전혀 스위칭되지 않아 하프브릿지 회로가 완성되지 않고 → 모터 단자에 전압이 형성되지 않을 수 있습니다.

→ 확인 방법: 이 보드가 6개 입력 모두 MCU에서 오는 구조인지(즉 BLDCDriver6PWM이 필요한 보드인지), 아니면 LIN1-3#이 고정 레벨(예: 항상 GND/항상 대기)로 묶여 3핀만으로 동작하도록 설계됐는지 실물 기판에서 LIN1#/2#/3# 핀의 배선을 직접 추적해보시는 걸 권장합니다. (텍스트 추출로는 이 부분 라벨이 명확히 안 잡혔습니다.)

3순위: IO32/33/25 핀 매핑 자체가 틀렸을 가능성
schematic 텍스트상 PA0(→HIN1), PB0(→HIN2), PC0(→HIN3) 넷 라벨이 ESP32 모듈의 8/9/10번 핀(IO32/IO33/IO25) 근처에 위치한 것으로 보이긴 하나, PDF 텍스트 추출이 일부 깨져 있어 100% 확신하기 어렵습니다.

→ 확인 방법: 오실로스코프나 멀티미터로 IO32/33/25 핀에서 실제 PWM 파형이 나오는지, 그리고 그 신호가 U9의 HIN1/2/3 핀까지 실제로 연결(continuity)되는지 직접 찍어보시는 게 텍스트 분석보다 훨씬 확실합니다.

추가로 체크할 기본 항목
LED2(10V 인디케이터), LED1(VIN 인디케이터)가 켜져 있는지 — DC-DC 10V(U2 BL9342)가 정상 동작해야 게이트드라이버 VCC가 삽니다.
driver.voltage_limit, driver.init(), driver.enable() 호출 순서가 정상인지.
VIN 커넥터(J1)에 실제 DC 전원이 들어가고 있는지(당연해 보여도 종종 원인).
결론적으로 가장 먼저 의심할 곳은 PWM 핀 번호보다 M_EN 계열의 파워 인에이블 신호입니다. "전혀 전원이 안 들어온다"는 증상은 PWM 핀 오배선보다 전원 인에이블 미설정 쪽이 훨씬 전형적인 패턴이에요.