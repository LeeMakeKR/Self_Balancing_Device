# 제작 로그

20260718 esp32 foc 보드 테스트 및 점검. mks esp32 foc 보드와 dengfoc 보드 배선이 동일함을 확인. 
 - mega 보드의 enable 핀은 GPIO12(중요)
 - 3720 bldc 모터(안마건용)은 3800rpm이 한계로 파악됨

-2808 bldc 모터(RC용)는 4500rpm 이상 회전 가능함을 확인. 하지만 발열 문제가 있고 볼트축으로 고정하는 구조라 밸런스휠의 단단한 고정이 힘듦

- ProDrone 4212 모터로 결정



- esp32 foc 각 보드는 모터 제어용으로만 사용하고, 메인 esp32 보드가 can 통신으로 각 모터를 제어.
 그리고 bmi270 6개도 메인보드에 배치된다. 

 

 20260724 
 - 자이로센서의 배치 최적화
 
 2060731
 1축 조립하여 테스트하고, can 연결하여 메인보드가 개별축을 컨트롤하는 구조 테스트 에정
 1축에서는 as5047을 이용하여 모터의 회전과 각도를 확인하고, 모터의 회전을 높여가면서 최대 rpm 확인. 

 - 브레이크가 영 해결이 안됨. 일단 자이로만 제작하고 브레이크는 나중에 추가할까?

 # 모터 특성 측정 결과

1축 프로토타입의 반작용 휠 구동계 실측 기록입니다. 측정에 쓴 스케치는 [TestCode/02_ramp_tuning](../TestCode/02_ramp_tuning/02_ramp_tuning.ino) 입니다.

---

## 1. 가속률(램프율) 한계 측정 — 2026-07-31

### 측정 목적

밸런싱 제어기가 반작용 휠에 요구할 수 있는 **최대 각가속도**를 실측으로 확정합니다.
이 값이 곧 제어 권한(control authority)의 상한이며, LQR 설계 시 입력 포화 한계로 들어갑니다.

### 하드웨어 구성

| 항목 | 사양 |
|---|---|
| 드라이버 보드 | MKS ESP32 FOC Mega (ESP32-WROOM-32) |
| 모터 | ProDrone 4212 BLDC |
| 극쌍수 (PP) | **10** (initFOC의 PP check로 확정) |
| 자기각 센서 | AS5047P, SPI 10MHz |
| 부하 | 밸런스휠(자이로 휠) 장착 상태 |
| 공급 전압 | 12.0 V |
| 인가 전압 상한 | 11.0 V |

### 제어 설정

```cpp
motor.controller        = MotionControlType::velocity;
motor.torque_controller = TorqueControlType::voltage;   // 전류센싱 미사용
motor.foc_modulation    = FOCModulationType::SpaceVectorPWM;
motor.PID_velocity.P    = 0.1f;
motor.PID_velocity.I    = 1.0f;
motor.LPF_velocity.Tf   = 0.02f;
```

- 센서 방향: CW (+1)
- zero electric angle: 3.7015

### 측정 방법

목표 3000 RPM에 대해, 지령 램프율을 200 RPM/s부터 1.6배씩 올려가며
0 → 목표 95%(2850 RPM) 도달 시간(t95)과 지령-실제 최대 괴리(max lag)를 측정.
시행 사이에는 드라이버를 끄고 자유 감속으로 정지.

### 원본 결과

```
[2] HEALTH CHECK
  motor rotated    : YES  (peak 2860 RPM)
  sensor readings  : OK   (total 1707.2 rev)
  trials run       : 9  (5 ok / 4 rejected)

[3] TRIAL TABLE
  ramp[RPM/s]   t95[ms]   maxlag   coast[s]   verdict
  200           14043     77       3.8        ok
  320           8784      242      2.6        ok
  512           5493      224      3.2        ok
  819           3462      224      2.8        ok
  1311          2166      263      2.8        ok  <== BEST
  2097          2239      1095     3.4        rejected
  3355          2180      1758     2.4        rejected
  5369          2152      2247     2.2        rejected
  8590          2236      2577     2.4        rejected
```

### 분석 — 실제 달성 가속률 역산

t95로부터 `2850 / t95` 를 계산하면 지령이 아닌 **실제** 달성 가속률이 나옵니다.

| 지령 [RPM/s] | t95 [ms] | 실제 달성 [RPM/s] | 추종 여부 |
|---|---|---|---|
| 200 | 14043 | 203 | 지령대로 |
| 320 | 8784 | 324 | 지령대로 |
| 512 | 5493 | 519 | 지령대로 |
| 819 | 3462 | 823 | 지령대로 |
| **1311** | **2166** | **1316** | **한계 지점** |
| 2097 | 2239 | 1273 | 포화 |
| 3355 | 2180 | 1307 | 포화 |
| 5369 | 2152 | 1324 | 포화 |
| 8590 | 2236 | 1275 | 포화 |

1311 RPM/s까지는 지령과 실제가 일치하고, 그 위로는 실제값이 **1275~1324 RPM/s**에
고정됩니다. 지령을 6배(8590)로 올려도 도달 시간이 줄지 않는다는 것은
전압/토크 한계에 걸렸다는 뜻입니다.

### 결론

```
최대 각가속도  α_max ≈ 1320 RPM/s ≈ 138 rad/s²
권장 지령 램프율      1311 RPM/s  (한계의 99%, 추종 유지)
```

권장값이 물리적 한계와 1% 이내로 일치하므로 **추가 튜닝의 실익이 없습니다.**
스윕을 더 촘촘히 해도 t95는 15ms 정도만 줄어듭니다.

### 제어 설계에 미치는 의미

1. 반작용 휠이 낼 수 있는 최대 토크가 `τ_max = J_wheel × 138 [rad/s²]` 로 묶입니다.
   J_wheel(휠 관성모멘트)을 별도로 구하면 τ_max가 절대값으로 확정됩니다.
2. 이 값은 0 → 2850 RPM 구간의 **평균**입니다. `TorqueControlType::voltage`에서는
   속도가 오를수록 역기전력이 인가전압을 잠식해 가용 토크가 줄어듭니다.
   저속에서는 138보다 크고, 고속에서는 작습니다.
3. 속도별 가용 토크 곡선이 필요하면 전류센싱(`foc_current`)을 붙여 재측정해야 합니다.

### 부수 확인 사항

- **최대 RPM**: peak 2860 RPM은 도달 판정선(2850)을 막 넘긴 값이라 진짜 최대치가 아닙니다.
  다만 11V에서 3000 RPM 부근이 상한으로 보입니다. 더 높은 휠 속도가 필요하면
  공급전압을 올려야 합니다.
- **모터/센서 정상 동작 확인**: 총 1707회전 동안 각도 추종이 유지되었고,
  9회 시행 전부 목표에 도달했습니다. 구동계와 센서 모두 정상입니다.

--