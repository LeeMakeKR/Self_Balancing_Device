# CAN 프로토콜 정의

Self_Balancing_Device 메인 보드(ESP32-S3)와 축별 서브 보드(ESP32 FOC) 사이의 CAN 통신 규격입니다.

- 작성일: 2026-08-03
- 상태: **설계 확정, 구현 전** (물리 계층은 검증 완료)

---

## 1. 물리 계층

| 항목 | 값 | 비고 |
|---|---|---|
| 비트레이트 | 500 kbit/s | |
| 프레임 형식 | 표준 프레임 (11bit ID) | 확장 프레임 미사용 |
| 트랜시버 | SN65HVD230 (3.3V) | 5V 인가 금지 |
| 메인 보드 핀 | GPIO1(TX) / GPIO2(RX) | 실배선 확인 완료 |
| 종단저항 | 버스 양 끝 120Ω | 모듈 실장분 중복 주의 |
| RS 핀 | GND 직결 (고속 모드) | VCC 연결 시 송신 정지 |

**필수**: 모든 보드의 GND를 공통으로 연결할 것. CANH/CANL만 이으면 동작하지 않습니다.

### 검증 결과 (2026-08-03, 2노드 왕복 테스트)

| 항목 | 측정값 |
|---|---|
| 전송률 | 200/200 fps (목표 200Hz 달성) |
| 유실률 | 0.0% (14초 연속) |
| 왕복 지연(RTT) | 평균 503us / 최대 522us |
| 지터 | 약 29us |
| 버스 에러 | txerr 0 / rxerr 0 |

RTT 503us는 물리 한계에 근접한 값입니다. 500kbit/s에서 8바이트 프레임 1개가 약 270us이므로 왕복 2프레임의 이론값이 약 540us이며, 소프트웨어 오버헤드는 사실상 0입니다.

---

## 2. 노드 구성

```
                  ┌─────────────────────────┐
                  │  MAIN (ESP32-S3)        │
                  │  IMU x6, 서보 x3        │
                  │  밸런스 제어 연산 @200Hz│
                  └───────────┬─────────────┘
                              │ CAN 500k
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
  ┌─────┴──────┐       ┌──────┴─────┐        ┌──────┴─────┐
  │ SUB 1축    │       │ SUB 2축    │        │ SUB 3축    │
  │ AS5047+모터│       │ AS5047+모터│        │ AS5047+모터│
  └────────────┘       └────────────┘        └────────────┘
```

### Node ID 부여

서브 보드 3장은 **동일 펌웨어**를 사용하고 축 번호만 다릅니다. 구분은 **IO34 / IO35 점퍼**로 합니다.

NVS 저장 방식 대신 점퍼를 쓰는 이유: 보드 고장 시 교체가 점퍼 이동만으로 끝나고, 설정 상태를 **눈으로 확인**할 수 있습니다.

### 배선 (외부 풀업 필수)

```
   3.3V ──[10kΩ]──┬── IO34 ── 점퍼 ── GND      IO34 = bit0
   3.3V ──[10kΩ]──┴── IO35 ── 점퍼 ── GND      IO35 = bit1

   점퍼 꽂음(GND 연결) = 0 / 점퍼 뺌(풀업으로 3.3V) = 1
```

**외부 10kΩ 풀업 저항 2개는 생략할 수 없습니다.** ESP32-WROOM-32의 GPIO34~39는 입력 전용이며 **내부 풀업/풀다운 회로가 물리적으로 존재하지 않습니다.** `pinMode(34, INPUT_PULLUP)`은 에러 없이 무시되고, 플로팅 상태의 핀은 주변 노이즈에 따라 0/1이 무작위로 읽힙니다. 이 상태로 두면 두 서브 보드가 같은 축 번호를 주장해 한 축은 명령을 못 받고 다른 축은 이중 응답하게 됩니다.

입력 전용 핀을 고른 것은 오히려 장점입니다. 펌웨어 실수로 출력 설정을 하더라도 GND 직결 점퍼와 단락될 수 없습니다.

### Node ID 표

점퍼를 꽂으면 GND에 연결되어 `0`, 점퍼를 빼면 풀업 저항에 의해 `1`로 읽힙니다.

| IO34 (bit0) | IO35 (bit1) | 값 (bit1 bit0) | Node ID | 축 |
|---|---|---|---|---|
| `0` (점퍼) | `0` (점퍼) | `00` | 1 | 1번축 |
| `1` (개방) | `0` (점퍼) | `01` | 2 | 2번축 |
| `0` (점퍼) | `1` (개방) | `10` | 3 | 3번축 |
| `1` (개방) | `1` (개방) | `11` | — | **설정 오류 → 부팅 거부** (`fault_code 0x08`) |

**점퍼가 전부 빠진 상태가 유효한 축 번호가 아니라 에러 상태로 떨어집니다.** 진동으로 점퍼가 이탈하는 것이 가장 흔한 실물 고장인데, 이 배치에서는 그것이 조용한 오동작이 아니라 명시적 부팅 실패로 나타납니다.

### 부팅 시 판정 절차

1. IO34 / IO35를 `INPUT`으로 설정 (`INPUT_PULLUP` 사용 금지 — 동작하지 않아 오해를 부릅니다)
2. 두 핀을 **10회 연속 읽어 전부 일치할 때만** 값을 확정 (노이즈 오독 방지)
3. 읽은 값이 `1 1`이거나 10회가 불일치하면 → 모터 출력 차단, `fault_code 0x08`로 `0x02n` 송신 후 정지
4. 확정된 Node ID로 자신의 CAN ID(`0x09n`, `0x02n`, `0x38n`) 결정
5. 확정된 Node ID를 시리얼에 출력 (배선 확인용)

### 사용 핀 충돌 확인

MKS ESP32 FOC Mega에서 IO34 / IO35는 미사용 핀입니다. 기존 배정과 겹치지 않습니다.

| 용도 | 사용 핀 |
|---|---|
| 3상 PWM | GPIO32, 33, 25 |
| 드라이버 Enable | GPIO12 |
| 전류 센싱 | GPIO39, 36 |
| AS5047 SPI | GPIO5(CS), 18(SCLK), 19(MISO), 23(MOSI) |
| CAN | GPIO15(TX), GPIO4(RX) |
| 기타 | GPIO2(LED), 13(VIN 측정), 16/17(UART1) |
| **Node ID (신규)** | **GPIO34, GPIO35** |

---

## 3. CAN ID 배정

CAN은 **ID가 낮을수록 우선순위가 높습니다.** 버스 경합 순서가 그대로 안전 순서가 되도록 배치했습니다.

| ID | 방향 | 주기 | 내용 |
|---|---|---|---|
| `0x010` | any → all | 이벤트 | **E-STOP** (최우선) |
| `0x021`~`0x023` | sub n → main | 이벤트 | 폴트 발생 보고 |
| `0x080` | main → all | **200Hz** | 3축 제어 명령 (브로드캐스트) |
| `0x091`~`0x093` | sub n → main | **200Hz** | 축별 텔레메트리 |
| `0x301`~`0x303` | main → sub n | 저속 | 파라미터 쓰기 |
| `0x381`~`0x383` | sub n → main | 10Hz | 확장 상태 |

`0x0n1`~`0x0n3`의 하위 자리는 Node ID(1~3)와 일치시켜, 수신 측에서 ID만 보고 축을 판별할 수 있게 했습니다.

**별도 heartbeat 메시지는 정의하지 않습니다.** 200Hz 텔레메트리 자체가 heartbeat 역할을 하므로 중복입니다.

---

## 4. 메시지 정의

모든 메시지는 **DLC 8 고정**입니다. 가변 길이를 쓰지 않는 이유는 수신 측 파싱 분기를 없애고 최악 부하를 확정하기 위해서입니다.

### 4.1 `0x080` 제어 명령 (main → all, 200Hz)

| 바이트 | 타입 | 필드 | 설명 |
|---|---|---|---|
| `[0]` | uint8 | `seq` | 0~255 롤링 카운터 |
| `[1]` | uint8 | `flags` | 아래 비트맵 참조 |
| `[2..3]` | int16 | `target[0]` | 1축 목표값 (LE) |
| `[4..5]` | int16 | `target[1]` | 2축 목표값 (LE) |
| `[6..7]` | int16 | `target[2]` | 3축 목표값 (LE) |

`flags` 비트맵:

| 비트 | 의미 |
|---|---|
| `0` | 1축 enable |
| `1` | 2축 enable |
| `2` | 3축 enable |
| `3` | 브레이크 (1 = 단락 제동) |
| `4..5` | 제어 모드 (`0` = 토크, `1` = 속도, `2..3` 예약) |
| `6..7` | 예약 (0으로 송신) |

`target` 단위:

| 모드 | 단위 | 범위 |
|---|---|---|
| 토크 | mA | ±32767 mA |
| 속도 | rad/s × 10 | ±3276.7 rad/s |

### 4.2 `0x09n` 텔레메트리 (sub n → main, 200Hz)

| 바이트 | 타입 | 필드 | 설명 |
|---|---|---|---|
| `[0]` | uint8 | `seq_echo` | 받은 `0x080`의 seq를 그대로 반향 |
| `[1]` | uint8 | `status` | 아래 비트맵 참조 |
| `[2..3]` | uint16 | `angle_raw` | AS5047 14bit 원본 (0~16383, LE) |
| `[4..5]` | int16 | `velocity` | 각속도 rad/s × 10 (LE) |
| `[6..7]` | int16 | `current` | 실측 전류 mA (LE) |

`status` 비트맵:

| 비트 | 의미 |
|---|---|
| `0` | 모터 enable 상태 |
| `1` | 폴트 발생 중 (상세는 `0x02n` 참조) |
| `2` | 엔코더 캘리브레이션 완료 |
| `3` | 워치독 타임아웃으로 정지됨 |
| `4..7` | 예약 |

**`seq_echo`가 핵심입니다.** 이 필드로 왕복 지연을 프레임 단위로 정확히 재고, 어떤 명령이 유실됐는지 특정할 수 있습니다. 04번 테스트에서 이미 검증된 방식입니다.

**각도는 스케일 변환 없이 14bit 원본을 보냅니다.** 변환하면 정밀도 손실이 생기고 단위 정의가 양쪽 코드에 중복됩니다. 변환은 메인 보드에서 한 번만 합니다.

### 4.3 `0x02n` 폴트 보고 (sub n → main, 이벤트)

폴트 발생 시 **즉시 1회** 송신하고, 상태가 지속되는 동안은 텔레메트리의 `status` bit1로 표시합니다. 같은 폴트를 반복 송신하지 않습니다.

| 바이트 | 타입 | 필드 |
|---|---|---|
| `[0]` | uint8 | `fault_code` |
| `[1]` | uint8 | `severity` (0=경고, 1=축 정지, 2=전체 정지 요청) |
| `[2..3]` | int16 | 관련 측정값 (전류/온도 등) |
| `[4..7]` | uint32 | 발생 시각 (ms, LE) |

`fault_code` 정의:

| 코드 | 의미 |
|---|---|
| `0x00` | 폴트 없음 |
| `0x01` | 과전류 |
| `0x02` | 과온 |
| `0x03` | 저전압 |
| `0x04` | AS5047 통신 실패 |
| `0x05` | 엔코더 캘리브레이션 미완료 |
| `0x06` | CAN 워치독 타임아웃 |
| `0x07` | 속도 초과 |
| `0x08` | Node ID 설정 오류 |

### 4.4 `0x010` E-STOP (any → all, 이벤트)

| 바이트 | 타입 | 필드 |
|---|---|---|
| `[0]` | uint8 | `reason` (`fault_code`와 동일 체계) |
| `[1]` | uint8 | `source_node` (0=메인, 1~3=서브) |
| `[2..7]` | — | 예약 (0) |

수신 즉시 **모든 서브는 모터 출력을 차단**합니다. 해제는 전원 재인가 또는 메인의 명시적 리셋 명령(`0x30n`)으로만 가능합니다. 자동 해제하지 않습니다.

### 4.5 `0x30n` 파라미터 쓰기 (main → sub n, 저속)

| 바이트 | 타입 | 필드 |
|---|---|---|
| `[0]` | uint8 | `cmd` (0x01=파라미터 쓰기, 0x02=폴트 리셋, 0x03=캘리브레이션 시작, 0x04=NVS 저장) |
| `[1]` | uint8 | `param_id` |
| `[2..5]` | float32 | 값 (LE) |
| `[6..7]` | — | 예약 |

`param_id` 예시: `0x10` 전류 PID Kp, `0x11` Ki, `0x20` 속도 PID Kp, `0x30` 전류 제한, `0x31` 속도 제한.

### 4.6 `0x38n` 확장 상태 (sub n → main, 10Hz)

200Hz 텔레메트리에 넣을 자리가 없는 저속 정보입니다.

| 바이트 | 타입 | 필드 |
|---|---|---|
| `[0]` | uint8 | 현재 `fault_code` |
| `[1]` | int8 | 보드 온도 (°C) |
| `[2..3]` | uint16 | DC 버스 전압 (mV/10, LE) |
| `[4..5]` | uint16 | 엔코더 에러 누적 카운트 (LE) |
| `[6..7]` | uint16 | CAN 에러 누적 카운트 (LE) |

---

## 5. 설계 결정 근거

### 5.1 명령을 축별로 나누지 않고 브로드캐스트 1프레임으로 묶은 이유

| 방식 | 프레임/초 | 버스 부하 |
|---|---|---|
| 축별 명령 3개 + 텔레메트리 3개 | 1200 | 약 31% |
| **브로드캐스트 1개 + 텔레메트리 3개** | **800** | **약 21%** |

부하 감소보다 중요한 이유가 하나 더 있습니다. **3축이 같은 제어 틱에서 계산된 값을 동시에 받습니다.** 3축이 서로 커플링된 밸런싱에서는 축별 명령 도착 시각이 어긋나는 것 자체가 제어 오차로 나타납니다.

### 5.2 애플리케이션 체크섬을 넣지 않은 이유

CAN 프레임에는 이미 15bit CRC가 있고 해밍거리 6을 보장합니다. 2노드 짧은 버스에서 XOR 체크섬을 추가해도 검출 능력이 의미 있게 올라가지 않습니다. **그 1바이트를 실제 데이터에 쓰는 편이 낫습니다.**

단, `seq`는 유지합니다. CRC는 "프레임이 깨진 것"을 잡지만 **"프레임이 통째로 사라진 것"은 못 잡습니다.** 그건 `seq`만이 잡습니다.

### 5.3 버스 부하 검산

```
프레임 1개 = 표준 ID(11bit) + 데이터 8바이트, 비트스터핑 최악 포함 약 130 bit
130 bit / 500,000 bit/s = 260 us

200Hz × 4 프레임 = 800 frame/s
800 × 260us = 208 ms/s = 20.8%
```

10Hz 확장 상태(30 fps)와 이벤트성 메시지를 더해도 **25% 미만**입니다. CAN 권장 상한 40~50% 대비 여유가 충분하며, 향후 축 추가나 주기 상향 여지가 있습니다.

---

## 6. 안전 규칙 (워치독)

밸런싱 장치는 통신이 끊긴 채 모터가 마지막 명령대로 계속 회전하면 사고로 이어집니다. **이 절은 선택 사항이 아닙니다.**

| 노드 | 감지 조건 | 동작 |
|---|---|---|
| 서브 | `0x080`을 **15ms**(=3프레임) 이상 미수신 | 모터 즉시 출력 차단, `status` bit3 설정 |
| 메인 | 특정 축 텔레메트리 **50ms** 이상 미수신 | 해당 축 폴트 처리, 서보 안전 자세, E-STOP 판단 |
| 양쪽 | `TWAI_STATE_BUS_OFF` 감지 | 모터 차단 후 `twai_initiate_recovery()` |
| 서브 | E-STOP(`0x010`) 수신 | 즉시 차단, 자동 해제 금지 |

### 임계값 근거

- **서브 15ms**: 5ms 주기 기준 3프레임. 1~2프레임 유실은 정상 범위의 일시적 현상일 수 있으나, 3프레임 연속 유실은 링크 장애입니다.
- **메인 50ms**: 서브가 자체 워치독(15ms)으로 이미 정지한 뒤에도 회복되지 않는 상황을 확정하기 위해 여유를 둔 값입니다.

### 링크 장애 감지 지표

04번 테스트에서 서브 보드 부재 시 다음이 관측됐습니다. 이 값들을 그대로 감지 신호로 사용할 수 있습니다.

- `twai_transmit()` 실패 급증 (ACK 미수신)
- `rx_error_counter` 상승 (테스트 시 135까지 도달)
- 서브 보드 부팅 후 **버스오프 없이 자동 회복됨** — 회복 로직이 정상 동작함을 확인

---

## 7. 제어 루프 내 타이밍 순서

```
[메인 보드 5ms 틱]
  1. IMU 6개 읽기 (SPI)
  2. 자세 추정 + 밸런스 제어 연산
  3. 0x080 송신          ← 계산 직후 즉시 송신
  4. 서보 3개 PWM 갱신
  5. 수신 큐 비우기 (0x09n 텔레메트리 처리)

[서브 보드]
  0x080 수신 인터럽트 → 목표값 갱신 → FOC 루프에 즉시 반영
  자체 주기로 0x09n 송신
```

측정된 RTT가 503us이므로 5ms 주기 안에서 통신 지연이 차지하는 비율은 약 10%입니다. 제어 대역폭에 미치는 영향은 무시할 수 있습니다.

---

## 8. 관련 문서

- [README.md](README.md) — 메인 보드 핀아웃
- [코드 구조.md](코드%20구조.md) — 전체 코드 구성
- [04_can_com_test_main.ino](TestCode/04_can_com_test_main/04_can_com_test_main.ino) — 물리 계층 검증 스케치 (메인)
- [04_can_com_test_sub.ino](TestCode/04_can_com_test_sub/04_can_com_test_sub.ino) — 물리 계층 검증 스케치 (서브)
- [sn65hvd230.pdf](documents/sn65hvd230.pdf) — 트랜시버 데이터시트
- [AS5047P.pdf](documents/AS5047P.pdf) — 엔코더 데이터시트
