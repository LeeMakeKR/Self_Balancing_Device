# Self_Balancing_Device 제작 계획서

## 프로젝트 개요
자이로 센서와 밸런스 휠 모터를 이용한 모서리 균형 장치 개발 프로젝트
simpleFOC를 사용한 제어. 
## 하드웨어 구성

### 핵심 부품
- MCU: ESP32-S3
- 자기각 센서: AS5047P
- IMU(자이로/가속도): ?
- 모터: BLDC 모터(반작용 휠 구동)
- FOC 드라이버: ? 


### 구성 방향
- 1축 프로토타입 부터 시작. 
- 이후 3축 확장 시 축별로 센서/드라이버/모터를 동일 구조로 복제
- 센서는 다축 확장성을 위해 I2C 고정 주소 센서보다 SPI 기반 센서를 우선 검토


### 드라이버 선택 
ESP계열 SIMPLEFOC  - mks ESP32 FOC Mega 보드로 결정. 


### IMU 선정

- IMU 6개를 사용. 메인보드에 연결됨
- 기종: **ICM-42688-P** 기본, **MPU-6500** 대안. 둘 다 TDK InvenSense 제품.
- 두 기종은 SPI 4선 + INT 구조와 핀 배정이 동일해 교체 시 하드웨어 변경이 없습니다. 상세 비교와 배선표는 [README.md](README.md)의 「IMU 센서 선정」 참고.
- ICM-42688-P를 기본으로 두는 이유: 자이로 노이즈 밀도가 MPU-6500의 1/3(0.0028 vs 약 0.01 dps/√Hz), 가속도는 1/4(65 vs 약 300 µg/√Hz) 수준입니다. 반작용 휠 진동이 그대로 실리는 구조라 노이즈 여유가 클수록 유리합니다.
- MPU-6500은 수급이 쉽고 저렴해 대안으로 둡니다. 다만 SPI로 쓸 때 `USER_CTRL`의 I2C 차단 비트를 반드시 세워야 하고, 레지스터 접근 클럭이 1 MHz로 제한됩니다.

원본 큐블리에서는 MPU6050 6개를 사용
ETH가 6개를 쓴 이유

큐블리는 꼭짓점(피벗)을 중심으로 회전하기 때문에, 가속도계는 중력뿐 아니라 원심·접선 가속도를 함께 측정합니다. IMU 1개로는 이 둘을 대수적으로 분리할 수 없습니다.
서로 다른 위치에 붙은 여러 개의 가속도계를 쓰면, 위치 차이에 따른 측정값 차이로 각속도·각가속도 항을 직접 풀어낼 수 있어서 자이로 적분 없이(=드리프트 없이) 기울기를 추정할 수 있습니다. ECC 2013 논문의 "accelerometer-based tilt estimator"가 바로 이 방식입니다.


### 자기각 센서 선정
- 1순위 옵션(최종 선택): AS5047P (가장 권장)
- 특징: 최대 28,000 RPM 고속 회전 대응, 14비트 분해능
- 통신: 고속 SPI 및 ABI 펄스 출력
- 선정 이유: 점프-업(Jump-up)을 위한 반작용 휠의 급제동 구간에서 모터 회전자 위치를 지연 없이 파악 가능
- 선정 이유: SimpleFOC 라이브러리와의 호환성이 높아 초기 구동 및 제어 튜닝에 유리

### 서보모터 브레이크

- 자이로 휠을 바로 붙잡는 것보다 일단은 모터를 붙잡는 형태로 제작하여 테스트. 


## 개발 단계

### Phase 1: 1축 균형 장치 개발 및 하드웨어 설계


#### MCU 선정

- 1순위: ESP32-S3
- 선정 이유: 연산 여유, 주변장치(SPI/PWM) 활용성, 개발 생태계


### 코드 구조 설계 원칙
로직의 개선 및 수정이 용이하도록 구조적으로 모듈화하여 개발합니다.


ETH Cubli는 모터 컨트롤러(Maxon DEC 36/2)에 **전류 설정값(current setpoint)**을 주는 방식을 사용했고, 토크는 Tm = Km·u (Km = 토크 상수, u = 전류) 관계로 모델링됩니다. LQR 제어기의 출력 자체가 각 모터의 전류값 u₁, u₂, u₃입니다. 즉, 밸런싱 제어 루프가 요구하는 것은 속도나 위치가 아니라 **토크(=전류)**입니다.

즉 simplefoc 라이브러리의 토크(전류 센싱)
