start_liveplotter(lambda:[odrv0.axis0.encoder.pos_estimate, odrv0.axis0.controller.pos_setpoint])

odrv0.axis0.requested_state = AXIS_STATE_CLOSED_LOOP_CONTROL

odrv0.axis0.controller.config.pos_gain = 25

odrv0.axis0.controller.config.vel_gain = 0.13

odrv0.axis0.controller.config.vel_integrator_gain = 1

odrv0.axis0.controller.input_pos = 10


가속도/속도 설정
# 제어 모드: 위치 제어
odrv0.axis0.controller.config.control_mode = CONTROL_MODE_POSITION_CONTROL

# 입력 모드: 사다리꼴 궤적(trap_traj)
odrv0.axis0.controller.config.input_mode = INPUT_MODE_TRAP_TRAJ

# 궤적 속도/가속도/감속도 설정
odrv0.axis0.trap_traj.config.vel_limit = 2000     # [turn/s]
odrv0.axis0.trap_traj.config.accel_limit = 80   # [turn/s^2]
odrv0.axis0.trap_traj.config.decel_limit = 80   # [turn/s^2]
dc_max_negative_current = -5
odrv0.save_configuration()
odrv0.reboot()

감속 때 발생하는 회생 에너지를 어떻게 처리할 것인가

odrv0.config.enable_brake_resistor
모터 에러시

# 2. 모든 에러 플래그 초기화
odrv0.axis0.clear_errors()

# 3. 폐루프 제어(모터 구동) 상태로 진입 요청
odrv0.axis0.requested_state = AXIS_STATE_CLOSED_LOOP_CONTROL


odrv0.config.dc_max_negative_current = 0    # 배터리/전원 쪽으로는 회생 전류를 전혀 허용하지 않음
odrv0.config.brake_resistance = 2.0         # 브레이크 저항은 반드시 켜둬야 함 (0으로 하면 갈 곳이 없어져 에러남)


dump_errors(odrv0)
